#include "stack.h"

int main(int argc, char const *argv[])
{
    Stack<string> *stack;

    cout << stack->get_index();

    // if(stack->push("!!!"))
    //     cout<< "INSERTADO" << endl;
    // else
    //     cout << "NO INSERTADO!!!" << endl;
    // stack->push("World");
    // stack->push("Hello");

    // string dato = stack->pop();
    // stack->seek();
    // cout << dato;

    return 0;
}
